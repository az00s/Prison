@model Prison.App.Web.Models.PositionViewModel

@{
    ViewBag.Title = "Удалить";
}

<h3>Вы действительно хотите удалить эту должность?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        

       

        <dt>
            @Html.DisplayNameFor(model => model.PositionName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PositionName)
        </dd>

    </dl>

    @using (Html.BeginForm("DeleteFromDb", "Position", new { id = Model.PositionID })) {
                @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <div class="btn-group">
            @Html.ActionLink("Назад", "Index", null, new { @class = "btn btn-default" })
            <input type="submit" value="Удалить" class="btn btn-danger" />
        </div>
    </div>
    }
</div>
